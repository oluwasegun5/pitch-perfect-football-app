package com.localhost.pitchperfect.domain.model;

/**
 * Enum representing the possible statuses of a match.
 */
public enum MatchStatus {
    SCHEDULED,
    LIVE,
    COMPLETED,
    CANCELLED,
    POSTPONED
}
